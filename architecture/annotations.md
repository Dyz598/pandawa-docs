Pandawa uses PHP attributes but here we call it `annotations` to register parts of our code
to the application such as `routes` and `services`. The `annotation-bundle` registers
this feature to the application.